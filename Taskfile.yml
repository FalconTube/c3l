# https://taskfile.dev

version: '3'


tasks:
  default:
    cmds:
      - task: build
    silent: true

  build:
    cmds:
      - go build -ldflags="-s -w" -o cll main.go
    silent: true

  upx:
    cmds:
      - go build -ldflags="-s -w" -o cll main.go && upx cll
    silent: true
